type Props={
    name: string,
    message: string,
    counter: number,
    tagName:string,
    classes: [...string[]],
    text?: string,
    icon?: string,
    id: string,
    parentId:string,
}

export {Props}
