type Props={
    name: string,
    message: string,
    counter: number,
    tagName:string,
    classes: [...string[]],
    text?: string,
    icon?: never,
    id: string,
    parentId:string,
}

export {Props}